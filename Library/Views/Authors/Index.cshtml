<h1>Library</h1>
<hr />
<h2>Authors</h2>

@if (Model.Count != 0)
{
  <ul>
    @foreach (var author in Model)
    {
      <li><a href="/authors/@author.GetId()">@author.GetName()</a></li>
    }
  </ul>
}

<h4><a href="authors/new">Add an author to the catalog</a></h4>
<h4><a href="/">Home</a></h4>
